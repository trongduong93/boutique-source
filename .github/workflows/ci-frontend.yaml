name: CI Pipeline for Frontend

on:
  push:
    paths:
      - 'src/frontend/**'  # Trigger khi có thay đổi trong thư mục frontend
  pull_request:
    paths:
      - 'src/frontend/**'  # Trigger khi có thay đổi trong thư mục frontend

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        id: build_image
        run: |
          SERVICE_NAME="frontend"
          IMAGE_NAME=trongduonghsht/trongdd-${SERVICE_NAME}:ver-$(git rev-parse --short HEAD)          
          docker buildx build --no-cache -t $IMAGE_NAME ./src/$SERVICE_NAME --push
          echo $IMAGE_NAME

      - name: Install yq
        run: |
          sudo wget https://github.com/mikefarah/yq/releases/download/v4.15.1/yq_linux_amd64 -O /usr/bin/yq
          sudo chmod +x /usr/bin/yq

      - name: Update Helm Chart with new image
        run: |
          SERVICE_NAME="frontend"
          IMAGE_NAME=trongduonghsht/trongdd-${SERVICE_NAME}:ver-$(git rev-parse --short HEAD)
          
          # Clone the helmchart repo
          git clone https://github.com/trongduong93/boutique-helmchart.git
          cd boutique-helmchart/helm-chart
          
          # Update values.yaml with the new image using yq
          yq eval ".${SERVICE_NAME}.image = \"${IMAGE_NAME}\"" -i values.yaml
          
          # Commit and push the updated values.yaml
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git remote set-url origin https://trongduong93:${{ secrets.GH_PAT }}@github.com/trongduong93/boutique-helmchart.git
          git add values.yaml
          git commit -m "Update ${SERVICE_NAME} image to ${IMAGE_NAME}"
          git push
